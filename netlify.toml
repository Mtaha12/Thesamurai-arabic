[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NODE_ENV = "production"

# Add this for serverless functions support
[functions]
  directory = ".next/functions"

[[plugins]]
  package = "@netlify/plugin-nextjs"
